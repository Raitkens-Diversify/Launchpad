import { LightningElement } from 'lwc';

/**
 * Author: Mile Cacanovic
 *
 * card — reusable presentational surface (background + border + radius). Content goes
 * in the default slot. Padding defaults to 24px and is overridable per usage via the
 * `--card-padding` custom property set on the <c-card> element.
 */
export default class Card extends LightningElement {}