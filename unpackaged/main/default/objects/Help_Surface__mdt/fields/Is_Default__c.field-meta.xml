<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Is_Default__c</fullName>
    <defaultValue>false</defaultValue>
    <description>The surface links resolve to when there is no site context (the Admin Console&#39;s Copy link runs in Lightning) or the current network has no row of its own: ResourceCenterService.defaultSurface(). Exactly one row should be flagged (Arc). Several flagged → the first by DeveloperName.</description>
    <fieldManageability>DeveloperControlled</fieldManageability>
    <label>Is Default</label>
    <type>Checkbox</type>
</CustomField>
