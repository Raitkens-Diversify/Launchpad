<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Exploratory_Session__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>The session this finding was logged during. Null = logged directly in the cycle log (including every pre-session finding). SetNull: deleting a session preserves its findings — findings are the durable output.</description>
    <label>Exploratory Session</label>
    <referenceTo>Exploratory_Session__c</referenceTo>
    <relationshipLabel>Findings</relationshipLabel>
    <relationshipName>Findings</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
