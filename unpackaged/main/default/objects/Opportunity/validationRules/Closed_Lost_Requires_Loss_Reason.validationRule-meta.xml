<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Closed_Lost_Requires_Loss_Reason</fullName>
    <active>true</active>
    <description>Closed Lost Opportunity requires a Loss Reason</description>
    <errorConditionFormula>AND(
    OR(
        ISPICKVAL(StageName, &quot;Closed Lost&quot;),
        ISPICKVAL(StageName, &quot;Advisor Lost&quot;)
    ),
    ISBLANK(TEXT(Loss_Reason__c))
)</errorConditionFormula>
    <errorMessage>Closed Lost Opportunity requires a Loss Reason</errorMessage>
</ValidationRule>
