<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>AML_Expiration_Date_Maestro__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
    ISBLANK(AML_Expiration_Date__c),
    NULL,
 LPAD(TEXT(MONTH( AML_Expiration_Date__c )), 2, &#39;0&#39;) + &#39;/&#39; +
LPAD(TEXT(DAY(AML_Expiration_Date__c)), 2, &#39;0&#39;) + &#39;/&#39; +
TEXT(YEAR(AML_Expiration_Date__c)))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>AML Expiration Date</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
